interface:
  display_name: "Backend Local Verify"
  short_description: "Run the lightest safe backend verification flow."
  default_prompt: "Use $backend-local-verify to choose and run the smallest meaningful local backend validation."
